You are Gemini, Chief Principal Engineer, under Claude (Head of FE). Working dir is this repo; local server http://localhost:8092. Read GEMINI-NOW.md. Full creative liberty on visual design — but this task is ADDITIVE/FIX only, do NOT delete or shorten any existing content.

BACKGROUND-PROCESS RULE: you CANNOT run shell commands (git/node/npm crash). Make FILE EDITS ONLY on index.html. Do NOT run git/node. Claude verifies + commits. When done, STOP.

Fix index.html (the homepage), preserving ALL existing content:

1. RESTORE 3 DROPPED CARDS in the "Operational standards buyers expect" section. The production/baseline version has these cards that are now MISSING: an ACCESSIBILITY card (WCAG/AA commitment), a SECURITY/ENCRYPTION card (AES-256 at rest, TLS 1.3 in transit, ISO 27001 controls, Cloudflare WAF), and the fuller AUDIT/evidence card. Re-add them as cards matching the existing card style in that section (UK Infrastructure / Statute-Accurate Intelligence / Built for Reliability / Developer surface). Keep all current cards too.

2. CAROUSEL ORDER: the product carousel currently leads with the Core (core-properties / core-analytics) screenshot. Reorder so it LEADS with CrowCyber, CrowCash, CrowMark (the revenue products), then CrowESG, then Core/CSRD. The guard requires the hero/first-carousel image to NOT be core-properties/core-analytics.

3. CONTRAST: fix the invisible/low-contrast text on index — INVISIBLE 1:1 "For your role", "IT & Cyber Lead" (role selector), and any 9px chips below 4.5:1. Light text on dark, dark text on light.

4. STORYTELLING (general, where it helps): where a dense text block would tell the story better as an interactive image/diagram, build that — but never remove the underlying facts.

TRUTH: £ only; never name individuals (use "CrowAgent Ltd, Companies House No. 17076461"); MEES ≤ £150,000; Band C 2028 "proposed"; PPN 002 = 10%; no fabricated customers/testimonials/trust-bands. Edit index.html only — then STOP. Claude commits.
