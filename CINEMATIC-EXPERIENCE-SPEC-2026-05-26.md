# Cinematic Experience Enforcement Spec (CTO mandate 2026-05-26)

**Target: TOP 1% flagship digital experience** — Apple-launch / Arc / Stripe / Linear / Framer-award grade. "Clean modern SaaS" is BASELINE, not the goal. Applied on top of `HOMEPAGE-TRANSFORM-PLAN-2026-05-26.md` + `TRANSFORMATION-SPEC.md`. Local-only on branch; no push without `APPROVED FOR PUSH — main`.

## Enforceable standards (verify each section against these)
### Motion = narrative, not decoration
- Every animation has purpose: directs attention, paces emotion, controls hierarchy, builds anticipation, creates memory.
- Build a COHERENT motion ARCHITECTURE: reusable primitives, synchronized timing tokens, GPU-friendly, interruption-safe, reduced-motion that stays premium. NOT stacked random GSAP snippets.
- Scroll choreography: cinematic section transitions, rhythm variation, density contrast, silence/breathing moments, progressive reveals, scroll-linked storytelling, spatial continuity.
- FORBIDDEN: generic fade-up spam, repetitive stagger, random floating elements, glow spam, gimmicks, motion without narrative value.

### Atmosphere & depth (no flat/dead sections)
- Every major section: atmosphere + environmental depth + compositional layering + intentional light + premium contrast + dimensionality.
- Foreground/midground/background separation; layered gradients, cinematic noise, subtle parallax, depth fields, intelligent masking, motion-reactive environments — restrained but rich, physically grounded.
- FORBIDDEN: flat backgrounds, empty dead space, generic gradients, lazy blur overlays, disconnected layers.

### Micro-interactions (tactile, engineered)
- Hover/cursor/button/card/nav/modal/focus/reveal/kinetic-typography all feel tactile, responsive, intentional. Nothing instant/flat/abrupt. Magnetic + spring easing where it serves.

### Systemic / automation
- Automate repetition: centralized motion orchestration, reusable animation utilities, unified timing tokens, responsive scaling logic, visual-consistency checks. Solve systemically; never manually patch the same thing forever.

## Quality gate (ask before finalizing ANY section)
Cinematic? immersive? emotionally directed? physically grounded? memorable? compositionally intentional? flagship-grade? — If it feels engineered/templated/static/ordinary/safe → keep pushing.

## Honesty constraint (unchanged, overrides "more impressive")
Dynamic ≠ fabricated. NO fake metrics/dashboards/customers/logos/testimonials/scale claims. Premium feeling comes from precision, typography, interaction quality, real statutory authority, restraint. Use REAL product screenshots (capture via test user; avoid embedded scale claims like "89 properties").

## How this maps to the transform plan
- T6 (compositional grid), T7 (motion choreography) now carry these standards.
- New cross-cutting tracks: **C-MOTION** (motion architecture: tokens + primitives + orchestrator), **C-DEPTH** (per-section atmosphere/depth layer), **C-MICRO** (micro-interaction system). Apply per-section as each is transformed; verify against the quality gate + reduced-motion + 60fps.
