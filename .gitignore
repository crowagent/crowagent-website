node_modules/
coverage/

.vercel/*
!.vercel/project.json
.wrangler/
.claude/

# Secrets — DEF-002 / DEF-003 — never commit any env file
.env
.env.*
.env.local
.env*.local
.env.development
.env.development.local
.env.production
.env.production.local
.env.test
.env.test.local

# OG image generator font cache (re-fetched on each clean build)
.cache/
