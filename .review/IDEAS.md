# PREMIUM ENHANCEMENT IDEAS (Claude → Gemini, head-of-FE backlog)
*Claude keeps adding. Apply where they ELEVATE the page; always reduced-motion-safe + 60fps; restraint over spam. On-brand = obsidian #040E1A + teal #0CC9A8, sovereign/cinematic.*

## Surface & finish (the "glassy" premium)
- **Glassmorphism, used with intent** (Apple "Liquid Glass" 2026): frosted nav + key cards — `backdrop-filter: blur()` + subtle 1px teal border + inner top highlight. Layered translucency for depth hierarchy, not decoration.
- **Specular sheen on hover** — a soft diagonal light sweep across cards/buttons on hover.
- **Layered elevation + soft inner shadows** (Apple depth), not flat boxes.
- **Animated gradient/conic borders** on the hero CTA or the "primary" product card.
- **Fine film grain + vignette** overlay for cinematic texture (very low opacity).

## Motion — slow, cinematic, premium (not fade-up spam)
- **Auto-playing title sequence** on load: ignition glow → masked line-by-line headline reveal (clip-path) with slight motion-blur → staggered eyebrow/sub/CTA → chips cascade.
- **Scroll-scrubbed storytelling**: pin a section, tie its animation to scroll progress (GSAP ScrollTrigger scrub) — slow, controlled, rhythmic reveal of the "how it works" steps.
- **Slow parallax depth** between background atmosphere, mid cards, and foreground text.
- **Counters that tick up slowly** on the REAL statutory facts (44 CE questions, 10% PPN 002, £150k cap) as they enter view.
- **Kinetic typography**: variable-font weight/width morph on scroll; key words mask-reveal.
- **Section theme transitions**: smooth dark↔light band crossfades as you scroll.

## Background / atmosphere (signature)
- **WebGL/CSS flowing mesh-gradient** (Stripe-grade) or the existing `signature-atmosphere` aurora — slow drift, teal.
- **Particle flow-field / "data current"** on the hero — on-brand "compliance intelligence in motion" (a `signature-flowfield` already exists to reuse).
- **Animated "compliance graph"**: SVG line-draw connecting a statute → the product output ("cited to SI 2015/962") — literal visual of the moat.
- **God-rays + bloom + depth fog** for a filmic hero; subtle slow camera push-in (scale).

## Interaction & automation
- **Magnetic / cursor-aware CTAs** + a soft cursor-follow spotlight glow.
- **3D tilt/parallax cards** (cursor-reactive) for product/sector cards.
- **Premium auto-advancing carousels**: browser-chrome frame, crossfade, dot tabs, pause-on-hover, off-screen pause, reduced-motion freeze.
- **Smart sticky nav**: hide-on-scroll-down / reveal-on-up, frosted glass at >60px, scroll-progress bar.
- **Hover-expand product cards**; animated nav underline; Lottie micro-icons.

## Signature "cited-to-statute" credibility (truthful, premium)
- **Live API/terminal code block** returning `// cited to SI 2015/962 reg 38` (technical authority, zero fabrication).
- **Statute chips** drifting subtly; a "scan beam" sweeping a precision grid (authority motif).

## Reference styles (owner-approved)
- `localhost:5173/variation-vercel.html` (near-black, white→fade gradient headline, code credibility) · `/variation-linear.html` (monumental white type, restraint, status pill). Keep teal as the single CrowAgent accent.

> Rule: every effect must be reduced-motion-safe, GPU-friendly (transform/opacity), paused off-screen, no CLS, and must never bury the content. Cinematic, calm, expensive — never gaudy.
