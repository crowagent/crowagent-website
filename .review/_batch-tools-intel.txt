You are Gemini, working under Claude (Head of FE) on the CrowAgent website. Working dir is this repo; local server http://localhost:8092. Read GEMINI-NOW.md for context and tone. Full creative liberty on design.

CRITICAL: You are in a background process that CANNOT run shell commands (git/node/npm crash with a ConPTY error). Make FILE EDITS ONLY. Do NOT run git, node, npm, or any shell command. Claude runs the guard + a11y + commits after you finish. When done, STOP.

TASK: Migrate these free-tool + intel pages to the Sovereign v2 design system. Template = `glossary/csrd.html` + `glossary/ppn-002.html` (already v2): head loads `sovereign-core-v2.compiled.css` + `signature-atmosphere-2026-05-26.css` + gsap/ScrollTrigger + `sovereign-transformation-v2.js` + `nav-inject.js`; body `bg-[#040E1A]`; `<header id="ca-nav" class="sv-nav">`; `<main id="main-content" class="ca-main-transformation">` with a `ca-hero ca-section-dark` hero, `py-40 bg-white text-[#040E1A]` light sections, a `ca-section-dark` CTA, then `<div id="ca-footer"></div>`.

FILES (do ALL, one at a time):
- tools/csrd-applicability-checker/index.html
- tools/cyber-essentials-readiness/index.html
- tools/late-payment-calculator/index.html
- tools/mees-risk-snapshot/index.html
- tools/ppn-002-calculator/index.html
- tools/vsme-materiality-light/index.html
- intel/cyber-essentials-tracker/index.html
- intel/mees-tracker/index.html

CRITICAL FOR THESE PAGES: they contain interactive calculators / forms / inputs with JS hooks. PRESERVE every form field, input, select, button, id/class hook, and the JS <script> tags EXACTLY — only restyle the page shell and surrounding layout into v2. Do NOT remove or rename any interactive element or script. The Cyber Essentials readiness page question IDs must stay identical.

RULES: preserve ALL content + SEO/JSON-LD; never delete content/functionality; light text on dark, dark text on light (never dark-on-dark). Truth: £ only; never name individuals (use "CrowAgent Ltd, Companies House No. 17076461"); MEES ≤ £150,000; Band C 2028 "proposed"; PPN 002 = 10%; no fabricated proof. Edit only — then STOP. Claude commits.
