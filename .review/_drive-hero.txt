You are Gemini, Chief Principal Engineer, under Claude (Head of FE). Working dir is this repo; server http://localhost:8092. Read GEMINI-NOW.md. FULL creative liberty — this is your craft. Owner-loved references: variation-vercel.html + variation-linear.html.

BACKGROUND-PROCESS RULE: you CANNOT run shell commands (git/node crash). FILE EDITS ONLY on index.html. Claude verifies + commits.

TASK: Transform the index.html HERO section into a genuinely top-1%, cinematic hero with rich ANIMATION + AUTOMATION. Keep ALL existing hero content (the "Win contracts. Protect your business. Get paid faster." headline, the "THE COMPLIANCE OPERATING SYSTEM" eyebrow, the subtitle, the CTAs, the product carousel, the cards). Elevate it with:
- ORCHESTRATED ENTRANCE: a staggered, choreographed reveal on load (eyebrow → headline lines → subtitle → CTAs → carousel), using GSAP (gsap/ScrollTrigger are already loaded). Smooth, premium easing.
- AUTOMATION: the product carousel must AUTO-ADVANCE (autoplay with a timed interval), pause-on-hover, with smooth slide/fade transitions and clean rounded corners (no edge-clipping). Consider a subtle animated accent (e.g. a slow live-feel metric or an animated underline) that makes it feel alive.
- AMBIENT MOTION: tasteful, restrained ambient movement (subtle gradient drift, soft glow on the headline, parallax on scroll) — but DO NOT reintroduce the busy .atmos mesh/aurora the owner disliked. Restrained and premium, like the mocks.
- MICRO-INTERACTIONS: magnetic/elevating CTA buttons on hover, smooth focus states.
- SCROLL CHOREOGRAPHY: as the user scrolls from hero into the next section, choreograph the transition (fade/translate reveals).
- It MUST be visible at scrollY:0 on load, fully accessible, with a prefers-reduced-motion fallback (no motion for those users).

CONSTRAINTS: light text on dark (never dark-on-dark); £ only; never name individuals; no fabricated metrics/customers. Edit index.html ONLY — then STOP. Claude pixel-verifies + commits.
