// premium-ui.js – helper for premium UI interactions
// This script runs after DOMContentLoaded (deferred) to initialize any JS‑based enhancements.

document.addEventListener('DOMContentLoaded', () => {
  // Ensure all .nt-reveal elements are active (fallback already added in HTML script)
  // Additional animations can be added here if needed.
  console.debug('Premium UI initialized');
});
