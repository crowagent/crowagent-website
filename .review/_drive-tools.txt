You are Gemini, under Claude (Head of FE). Working dir is this repo; local server http://localhost:8092. Read GEMINI-NOW.md. The reference for HOW to do this correctly is tools/csrd-applicability-checker/index.html — you already migrated it to v2 PERFECTLY (form + content fully preserved). Do the same for the others.

BACKGROUND-PROCESS RULE: you CANNOT run shell commands (git/node crash). FILE EDITS ONLY. Claude verifies + commits.

⚠️ ABSOLUTE RULE — PRESERVE EVERYTHING (a previous attempt GUTTED these pages to ~50% and was reverted): Keep the ENTIRE existing page body verbatim — every heading, paragraph, list, the calculator/form, EVERY <input>, <select>, <button>, every id/class/data- hook, and EVERY <script> tag. You are ONLY swapping the page SHELL to v2: change the <head> stylesheets to the v2 set (sovereign-core-v2.compiled.css + signature-atmosphere-2026-05-26.css + gsap + ScrollTrigger + sovereign-transformation-v2.js + nav-inject.js), set body to bg-[#040E1A], use <header id="ca-nav" class="sv-nav"> + <main id="main-content" class="ca-main-transformation"> + <div id="ca-footer">, and wrap the EXISTING content in v2 section/container classes. DO NOT rewrite, shorten, summarise, or rebuild the calculator. The page's word count must be ≥ the original. If you're tempted to delete anything, DON'T.

FILES (one at a time, each fully preserved):
- tools/cyber-essentials-readiness/index.html  (CRITICAL: the question IDs fw-001..pm-008 must stay IDENTICAL — they map to the platform bank)
- tools/mees-risk-snapshot/index.html
- tools/ppn-002-calculator/index.html
- tools/late-payment-calculator/index.html
- tools/vsme-materiality-light/index.html

TRUTH: £ only; never name individuals (use "CrowAgent Ltd, Companies House No. 17076461"); MEES ≤ £150,000; Band C 2028 "proposed"; PPN 002 = 10%; no fabricated proof. Light text on dark, dark text on light. Edit only — then STOP. Claude verifies each with the guard + a11y and commits; any gutted page will be reverted.
