{
  "name": "crowagent-website",
  "version": "1.0.0",
  "description": "",
  "main": "scripts.js",
  "scripts": {
    "prepare": "git config core.hooksPath .husky || true",
    "dev": "serve . -l 8092",
    "build:css": "node -e \"console.error('REFUSED. This used to run tailwind with -o pointed straight at Assets/css/sovereign-core-v2.compiled.css, the sheet 43 marketing pages load. A fresh build is NOT equivalent: it drops 472 utility rules, loses the srgb color-mix() fallbacks on every ca-* colour with an opacity modifier (they degrade to full opacity), and reintroduces the hardcoded hex/z-index/px values the token pass removed. Run `npm run check:css:sovereign` to see the measured delta and the fix order.'); process.exit(1)\"",
    "check:css:sovereign": "node scripts/check-sovereign-core-reproducibility.js",
    "build:css:legacy": "node .dev-tools/build-css-postcss.mjs styles.css styles.inlined.css && csso styles.inlined.css --output styles.min.css && node .dev-tools/purge-run.mjs && cp styles.purged.css styles.min.css && rm -f styles.inlined.css",
    "build:css:purge": "node scripts/build-css.js",
    "build:js": "node -e \"console.error(String.raw`REFUSED. This minified scripts.js into scripts.min.js. Both were deleted on 2026-09-07 with the rest of the legacy root JS cluster (A-258). Nothing has loaded either since the Cloudflare Pages deploy source moved to astro/dist on 2026-08-05. The site is astro/ and its build:deploy script.`); process.exit(1)\"",
    "build:og": "node scripts/generate-og-images.js",
    "build:og:force": "node scripts/generate-og-images.js --force",
    "build:og:check": "node scripts/generate-og-images.js --check",
    "build:webp": "node scripts/build-webp.js --rewrite-html",
    "build:webp:check": "node scripts/build-webp.js --check",
    "build:avif": "node scripts/build-avif.js",
    "build:avif:check": "node scripts/build-avif.js --check",
    "build:avif:force": "node scripts/build-avif.js --force",
    "build": "node -e \"console.error(String.raw`REFUSED. The legacy root tree was deleted on 2026-09-01. The site is astro/ and Cloudflare Pages runs its build:deploy script. Run that instead.`); process.exit(1)\"",
    "build:full": "npm run build:webp && npm run build:css && npm run build:js && npm run build:og",
    "//test": "REFUSED 2026-09-07 (A-258). Every jest suite in this repo loaded the legacy root JS cluster: crowagent.test.js, scripts.test.js and scripts-sw-register.test.js required ./scripts.js, and tests/unit covered cookie-banner.js and service-worker.js. That cluster was deleted, so the suite had nothing left to test and jest.config.js, whose only job was a per-file coverage floor over those four files, went with it. .husky/pre-push had already been emptied on 2026-09-01 for the same reason, having measured ZERO references to any of the four across all 46 built pages. The tests that guard what SHIPS are the gate chain in astro/ (npm run build there) and the Playwright specs under tests/, which jest never ran.",
    "test": "node -e \"console.error(String.raw`REFUSED. The jest suite and its config were deleted on 2026-09-07 (A-258) because every test in it loaded the legacy root JS cluster, which nothing has served since 2026-08-05. Run the gate chain instead: cd astro && npm run build. Playwright specs: npx playwright test.`); process.exit(1)\"",
    "//audit:release": "THE FREE LOCAL REPLACEMENT for .github/workflows/quality-gate.yml and lighthouse-ci.yml, both deleted 2026-09-01. Those two burned ~15 billed Actions minutes a week auditing the LEGACY root tree at URLs the live site 404s (/pricing.html, /contact.html) and had failed 18 consecutive runs since 2026-08-05. This runs axe-core over all 45 shipped astro/ routes and Lighthouse over one route per page template, on this machine, for zero Actions minutes. It is DELIBERATELY NOT WIRED INTO CI: under RULE 0-G wiring a gate into CI turns a free script into a blocking billable one and needs the owner's explicit approval. Do not add a workflow for it without asking. It never reads a Lighthouse exit code (chrome-launcher exits 1 on a clean audit on Windows because it cannot delete its temp profile) and it refuses to measure any route that does not land on itself with a 200.",
    "audit:release": "node scripts/audit-release.mjs",
    "audit:release:live": "node scripts/audit-release.mjs --live",
    "audit:ui": "node tests/ui-audit/run.js",
    "audit:ui:selftest": "node tests/ui-audit/self-test.js --live",
    "test:responsive": "playwright test tests/responsive.spec.js",
    "test:visual": "playwright test --project=visual-regression",
    "test:visual:update": "playwright test --project=visual-regression --update-snapshots",
    "test:cross-browser": "playwright test --project=cross-browser-chromium --project=cross-browser-firefox --project=cross-browser-webkit",
    "test:cross-browser:chromium": "playwright test --project=cross-browser-chromium",
    "sync-pricing": "node -e \"console.error('REFUSED, AND THE SCRIPT IS GONE RATHER THAN BROKEN. This ran scripts/sync-pricing-from-stripe.ts, which rewrote the LEGACY root pricing.html. Nothing has served that file since 2026-08-05, when the Cloudflare Pages deploy source moved to astro/dist, so every price it wrote landed on a page nobody reads. It was already inert before that: the BEGIN_PRICING_CARDS markers and the data-plan-tier attributes it edits no longer exist anywhere in pricing.html, so a real run exited 4 without writing a byte. Its tier vocabulary was stale in both directions, measured against live Stripe on 2026-08-05: it knew solo|team|agency, none of which carry a crowagent_* lookup_key at all, and it did not know portfolio, which IS the CrowMark top tier (crowagent_crowmark_portfolio_monthly, 54900 pence). It also mapped cards for CrowCyber, CrowCash and CrowESG, the three products R2.6.2 archived. RETIRED RATHER THAN RE-POINTED. The live figures are astro/src/data/pricing.ts, and that file exists precisely because the same number restated in four places disagreed; a fifth writer, a machine one, does not reduce restatement. Portfolio is deliberately NOT publicly priced (owner decision R241-PRICING-STRATEGY change 3) while Stripe holds an active 54900/592900 pair, so publishing what Stripe says is the one thing the pricing page must never do, and that is the default behaviour of any sync. TO CHANGE A PRICE: edit astro/src/data/pricing.ts and check it against Stripe by hand. KNOWN GAP, NAMED RATHER THAN HIDDEN: nothing now detects drift between the published figures and Stripe, and the stripe devDependency has no importer left. Script text is in git history at 5479aba4.'); process.exit(1)\"",
    "//build": "REFUSED 2026-09-01. This ran scripts/build-dist.js over the legacy root HTML tree, which the owner deleted once generate-og-images.js stopped reading it. The deploy is astro/ and its build:deploy script, which is what Cloudflare Pages runs. Nothing at the repo root builds a site any more."
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/crowagent/crowagent-website.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/crowagent/crowagent-website/issues"
  },
  "homepage": "https://github.com/crowagent/crowagent-website#readme",
  "devDependencies": {
    "@axe-core/playwright": "^4.11.3",
    "@playwright/test": "^1.59.1",
    "@resvg/resvg-js": "^2.6.2",
    "@tailwindcss/postcss": "^4.3.0",
    "autoprefixer": "^10.5.0",
    "csso": "^5.0.5",
    "csso-cli": "^4.0.2",
    "esbuild": "^0.28.1",
    "gsap": "^3.15.0",
    "jest": "^30.3.0",
    "jest-environment-jsdom": "^30.3.0",
    "lighthouse": "^13.3.0",
    "postcss": "^8.5.15",
    "postcss-import": "^16.1.1",
    "purgecss": "^8.0.0",
    "satori": "^0.12.2",
    "serve": "^14.2.6",
    "sharp": "^0.35.3",
    "stripe": "^17.5.0",
    "tailwindcss": "^4.3.0",
    "terser": "^5.46.1",
    "tsx": "^4.19.2"
  },
  "dependencies": {
    "@tailwindcss/cli": "^4.3.0",
    "typescript": "^6.0.3"
  },
  "overrides": {
    "qs": "^6.15.2",
    "esbuild": "^0.28.1"
  }
}
